# Build Scripts

Contains package and hosting build helpers.

- `prepare-sites-build.mjs`: assembles the Vite output, Worker entry point, and
  hosting manifest for Sites.

Read this directory only for build or deployment packaging work.

# Public Assets

Files here are copied unchanged into the client build. Product source does not
belong here; use this directory only for assets that require stable public URLs.

See `assets/README.md` before adding or changing media.
